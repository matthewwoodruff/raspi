# easy-rsa parameter settings

# NOTE: If you installed from an RPM,
# don't edit this file in place in
# /usr/share/openvpn/easy-rsa --
# instead, you should copy the whole
# easy-rsa directory to another location
# (such as /etc/openvpn) so that your
# edits will not be wiped out by a future
# OpenVPN package upgrade.

# This variable should point to
# the top level of the easy-rsa
# tree.
export EASY_RSA="{{ easy_rsa.EASY_RSA }}"

#
# This variable should point to
# the requested executables
#
export OPENSSL="{{ easy_rsa.OPENSSL }}"
export PKCS11TOOL="{{ easy_rsa.PKCS11TOOL }}"
export GREP="{{ easy_rsa.GREP }}"


# This variable should point to
# the openssl.cnf file included
# with easy-rsa.
export KEY_CONFIG={{ easy_rsa.KEY_CONFIG }}

# Edit this variable to point to
# your soon-to-be-created key
# directory.
#
# WARNING: clean-all will do
# a rm -rf on this directory
# so make sure you define
# it correctly!
export KEY_DIR="{{ easy_rsa.KEY_DIR }}"

# Issue rm -rf warning
echo NOTE: If you run ./clean-all, I will be doing a rm -rf on $KEY_DIR

# PKCS11 fixes
export PKCS11_MODULE_PATH="{{ easy_rsa.PKCS11_MODULE_PATH }}"
export PKCS11_PIN="{{ easy_rsa.PKCS11_PIN }}"

# Increase this to 2048 if you
# are paranoid.  This will slow
# down TLS negotiation performance
# as well as the one-time DH parms
# generation process.
export KEY_SIZE={{ easy_rsa.KEY_SIZE }}

# In how many days should the root CA key expire?
export CA_EXPIRE={{ easy_rsa.CA_EXPIRE }}

# In how many days should certificates expire?
export KEY_EXPIRE={{ easy_rsa.KEY_EXPIRE }}

# These are the default values for fields
# which will be placed in the certificate.
# Don't leave any of these fields blank.
export KEY_COUNTRY="{{ easy_rsa.KEY_COUNTRY }}"
export KEY_PROVINCE="{{ easy_rsa.KEY_PROVINCE }}"
export KEY_CITY="{{ easy_rsa.KEY_CITY }}"
export KEY_ORG="{{ easy_rsa.KEY_ORG }}"
export KEY_EMAIL="{{ easy_rsa.KEY_EMAIL }}"
export KEY_OU="{{ easy_rsa.KEY_OU }}"

# X509 Subject Field
export KEY_NAME="{{ easy_rsa.KEY_NAME }}"

# PKCS11 Smart Card
# export PKCS11_MODULE_PATH="/usr/lib/changeme.so"
# export PKCS11_PIN=1234

# If you'd like to sign all keys with the same Common Name, uncomment the KEY_CN export below
# You will also need to make sure your OpenVPN server config has the duplicate-cn option set
# export KEY_CN="CommonName"
